<!--ユーザーが出品した商品の詳細ページ　３つの画像を表示させなければならない。-->
<div class="main_container">
  <!-- サイドメニュー部分 -->
  <%= render partial: 'side_menu' %>
  
  <!-- 商品画像部分 -->
  <!--後にキーワード検索や価格表示をするとこちらのページを変える必要がある。-->
    <div class="main-wrapper">
      <ul>
            <li>
              <div class="product_name"><%= @product.name %></div>
                <% if @user.check_image(@product,1)%>
                  <div class="product">
                    <%= link_to item_edit_page_path(@product) do %>
                      <div class="image">
                          <%= image_tag "/images/#{@product.image1}"%>
                      </div>
                    <%end%>
                  </div>
                <%end%>
                <% if @user.check_image(@product,2)%>
                  <div class="product">
                    <%= link_to item_edit_page_path(@product) do %>
                      <div class="image">
                          <%= image_tag "/images/#{@product.image2}"%>
                      </div>
                    <%end%>
                  </div>
                <%end%>
                <% if @user.check_image(@product,3)%>
                  <div class="product">
                    <%= link_to item_edit_page_path(@product) do %>
                      <div class="image">
                          <%= image_tag "/images/#{@product.image3}"%>
                      </div>
                    <%end%>
                  </div>
                <%end%>
              <div class="button_wrap">
                  <%=link_to item_edit_page_path(@product) do %>
                      <button>編集する</button>
                  <%end%>
              </div>
            </li>
      </ul>
    </div>
</div>