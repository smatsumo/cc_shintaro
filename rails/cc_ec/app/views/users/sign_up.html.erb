<div class="sign_up">
  <div class="contents">
    <div class="sign_up_form">
      <h1>CodeCamp_EC</h1>
      <p>会員登録はこちらから</p>
      <%#会員登録のフォームでmethod: :postがない理由が知りたい。%>
      <%= form_for @user, url: sign_up_path do |f| %>
        <div class="field">
          <%= f.text_field :name, placeholder: "ニックネーム" %>
        </div>
        <div class="field">
          <%= f.email_field :email, placeholder: "メールアドレス" %>
        </div>
        <div class="field">
          <%= f.password_field :password, placeholder: "パスワード" %>
        </div>
        <div class="field">
          <%#f.check_password_filedが使えるかどうかの確証がない。%>
          <%= f.password_field :check_password, placeholder: "確認用パスワード" %>
        </div>
        <button>登録する</button>
      <% end %>
    </div>
    <div class="sign_in_link">
      <p>アカウントをお持ちですか？<%= link_to 'サインインする', sign_in_path %></p>
    </div>
  </div>
</div>

